[package]
name = "rushbuild"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust sibling of goshbuild for source-preserving bundle delivery"
repository = "https://github.com/runplus-community/rushbuild"

[dependencies]

